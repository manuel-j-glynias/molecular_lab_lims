import * as React from "react";


interface Props {
    variant_id: string
}
const GenomicMarkerCNVEditorContainer : React.FC<Props> = ({variant_id}) => {

    return <div>GenomicMarkerCNVEditorContainer</div>
}

export default GenomicMarkerCNVEditorContainer;